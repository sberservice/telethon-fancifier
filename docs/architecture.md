# 🏗️ Архитектура Telethon Fancifier

## 🎯 Цель

Сервис перехватывает исходящие сообщения пользователя в выбранных чатах и применяет к тексту цепочку модулей, после чего пытается отредактировать исходное сообщение.

## 🔄 Поток обработки

1. Telethon получает событие `NewMessage(outgoing=True)`.
2. Проверяется, что чат включён в настройки.
3. Проверяются safeguards:
   - сообщение действительно последнее ваше в этом чате;
   - возраст сообщения не более 10 секунд.
4. Текст передаётся в pipeline модулей в заданном порядке.
5. В `dry-run` выводится результат в консоль без Telegram API-запроса на правку.
6. В обычном режиме выполняется редактирование сообщения.

## 🧩 Плагинная модель

- Каждый модуль реализует единый интерфейс `Plugin`.
- Ядро ничего не знает о внутренней логике модулей, только вызывает `transform`.
- Встроенные модули лежат в `src/telethon_fancifier/plugins/`.
- Внешние пользовательские модули загружаются из `plugins/`.

## 🤖 LLM-провайдеры

- Введён абстрактный интерфейс провайдера (`BaseLlmProvider`).
- Текущая реализация: `DeepSeekProvider`.
- Для переключения на другой API достаточно добавить новый адаптер, не меняя ядро.

## 💾 Конфигурация

- Конфиг и состояние хранятся в пользовательской app-data директории.
- Схема имеет версию (`schema_version`) для будущих миграций.
- Хранятся:
  - выбранные чаты;
  - отображение `чат -> список модулей` в порядке применения;
  - флаги запуска (например, `dry_run` по умолчанию).

## 🛡️ Safeguards и устойчивость

- Таймаут редактирования: 10 секунд.
- Ограничение только на последнее сообщение пользователя в чате.
- Изоляция ошибок модулей: падение плагина не должно ронять daemon полностью.
