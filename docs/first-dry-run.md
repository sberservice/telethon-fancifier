# 🧪 Первый dry-run: от нуля до запуска

Ниже минимальный пошаговый сценарий, чтобы безопасно проверить работу демона без реального редактирования сообщений в Telegram.

## 1) Требования

- Python 3.12+
- Данные Telegram API (`api_id`, `api_hash`) с https://my.telegram.org

## 2) Клонирование и установка

### macOS / Linux

```bash
git clone <URL_ВАШЕГО_РЕПОЗИТОРИЯ>
cd telethon-fancifier
python3 -m venv .venv
source .venv/bin/activate
pip install -e .
```

### Windows (PowerShell)

```powershell
git clone <URL_ВАШЕГО_РЕПОЗИТОРИЯ>
cd telethon-fancifier
py -3 -m venv .venv
.\.venv\Scripts\Activate.ps1
pip install -e .
```

## 3) Настройка окружения

Скопируйте шаблон и заполните секреты:

### macOS / Linux

```bash
cp .env.example .env
```

### Windows (PowerShell)

```powershell
Copy-Item .env.example .env
```

Минимально обязательные переменные:

- `TELEGRAM_API_ID`
- `TELEGRAM_API_HASH`
- `TELEGRAM_SESSION_NAME`

Для модуля `llm_rewrite` также задайте `DEEPSEEK_API_KEY`.

## 4) Интерактивная настройка чатов и модулей

```bash
telethon-fancifier setup
```

Что делает команда:

1. Показывает меню мастера настройки.
2. Позволяет добавить/изменить чаты и порядок модулей.
3. Позволяет удалить ненужные чаты из локального конфига.
4. Показывает текущие привязки чатов к модулям.

## 5) Первый безопасный запуск

```bash
telethon-fancifier run --dry-run
```

В этом режиме Telegram-сообщения не редактируются: в консоли печатается исходный и результирующий текст.

## 6) Проверка текущей конфигурации

```bash
telethon-fancifier show-config
```

Команда выводит:

- список подключённых чатов;
- какие модули назначены на каждый чат;
- полный список доступных модулей.

## 6.2) Проверка LLM-ответа без Telegram

```bash
telethon-fancifier test-llm --text "Это тестовый текст"
```

Команда полезна для отладки `llm_rewrite`: она показывает фактический ответ модели, не запуская Telegram-демон.

## 6.1) Удаление ненужных чатов из конфига

```bash
telethon-fancifier remove-chats
```

Команда не обращается к Telegram API: удаляет выбранные чаты только из локального сохранённого конфига.

## 7) Переход в боевой режим

После проверки dry-run:

```bash
telethon-fancifier run
```

## 8) Автозапуск при входе в Windows

На Windows можно добавить задачу в Task Scheduler, чтобы демон стартовал автоматически при входе пользователя:

```bash
telethon-fancifier install-startup-task
```

Для безопасного старта в режиме проверки:

```bash
telethon-fancifier install-startup-task --dry-run
```

Если проект лежит не в текущей папке:

```bash
telethon-fancifier install-startup-task --project-dir "C:\\path\\to\\telethon-fancifier"
```

Удаление задачи:

```bash
telethon-fancifier remove-startup-task
```

Проверка статуса задачи:

```bash
telethon-fancifier startup-task-status
```

## ⚠️ Важные ограничения защиты

- Сообщение не редактируется, если прошло более 10 секунд с момента отправки.
- Редактируется только последнее ваше сообщение в конкретном чате.

## 🆘 Типовые ошибки и что делать

### Ошибка: не заданы `TELEGRAM_API_ID/TELEGRAM_API_HASH`

- Проверьте, что создан файл `.env` на основе `.env.example`.
- Убедитесь, что значения не пустые, а `TELEGRAM_API_ID` — целое число.

### Ошибка получения списка чатов

- Проверьте интернет-соединение.
- Проверьте корректность `api_id`/`api_hash`.
- Убедитесь, что сессия Telegram авторизована.

### Ошибка внешнего API (DeepSeek)

- Приложение не падает: модуль `llm_rewrite` вернёт исходный текст.
- Проверьте `DEEPSEEK_API_KEY`, `DEEPSEEK_BASE_URL`, `DEEPSEEK_MODEL`.

### Где смотреть детали

- При любой ошибке CLI показывает путь к лог-файлу.
- Стандартный путь: `app-data/telethon-fancifier/logs/app.log` (platform-specific).
